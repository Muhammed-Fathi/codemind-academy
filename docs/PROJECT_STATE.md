# CodeMind Academy Project State

## Phase status
- **Deployment Readiness Audit (Phase 1):** completed (2026-09-07). Baseline `635de56`. See `docs/DEPLOYMENT_READINESS_AUDIT.md`.
  - 1 CRITICAL security fix (privilege escalation via registration)
  - 2 HIGH security fixes (quiz answer leakage, parent impersonation)
  - All 337 test assertions still pass
- Phase 2: completed; `docs/curriculum/knowledge-model.json` is the source-aligned curriculum contract.
- Phase 3: completed — Database & Domain Model Foundation (PR #14 merged).
- **Production Build & Type Safety (Phase 2 of current cycle): completed (2026-09-07). Baseline `fce4821`. See `docs/PHASE_2_BUILD_TYPE_SAFETY.md`.**
  - `ignoreBuildErrors` removed — `next build` performs real TypeScript validation (67 pre-existing hidden errors fixed; `tsc --noEmit` now clean).
  - Build script is cross-platform: `prisma generate && next build && node scripts/copy-standalone-assets.mjs` (replaces the Unix-only `cp -r` chain; same standalone layout preserved).
  - Added `bun run typecheck` (`tsc --noEmit`). All 337 test assertions still pass; lint state unchanged (78 pre-existing issues deferred); production startup verified under node and bun.
- **Production Security Hardening (Phase 3 of current cycle): completed (2026-09-07). Baseline `5bd041c`. See `docs/PHASE_3_SECURITY_HARDENING.md`.**
  - `SECURITY_HASH_SECRET` is now REQUIRED in production: `src/lib/env.ts` validates it, `next build` and server startup (`src/instrumentation.ts`) fail fast with a value-free message; the hardcoded dev fallback is gone from `security.ts` (M3 closed).
  - Defense-in-depth proxy added (`src/proxy.ts`, Next 16 name for middleware): cookie-presence 401 for protected `/api/*` namespaces only, no DB, routes remain the authorization source of truth (M4 closed). Baseline security headers + `poweredByHeader: false`.
  - HIGH fix: public `/api/groups` no longer serialises teachers' full `User` rows (password hash, phone, e-mail).
  - Dependencies: nodemailer 7→9.1.1, sharp 0.34→0.35.4, next 16.1→16.3.4 (minor; fixes proxy-bypass/DoS advisories); removed 6 unused direct deps (`next-auth`, `next-intl`, `@mdxeditor/editor`, `react-syntax-highlighter`, `uuid`, `@reactuses/core`). `npm audit` 12 → 4 high (only `xlsx` no-fix + prisma-CLI transitive). Prisma unchanged.
  - Phase 2 carry-over closed: `examples/` excluded from `tsconfig.json`.
  - `start` script replaced by dependency-free `scripts/start-production.mjs` (Windows-portable, same Linux behaviour).
  - Tests: 337 baseline + 242 new security assertions = 579 passing; typecheck 0 errors; build PASS.
- **Student Learning & Session Progression (Phase 4 of current cycle): completed (2026-09-08). Baseline `df81de4`. See `docs/PHASE_4_STUDENT_PROGRESSION.md`.**
  - CRITICAL fix: `/api/quizzes/[id]` (+ `/start`, `/submit`, `/evidence`) had **no session gate** — a student could `POST .../submit` with empty answers for a session two steps ahead, pre-satisfying the quiz requirement of every future session and receiving every correct answer/explanation. All four routes now go through the new `canAccessQuiz`.
  - Locked sessions no longer leak protected content: `/api/courses/[slug]` redacts `videoUrl`, `pdfUrl`, `summary`, `description`, `quiz`, `homework` and `requirements` (presence flags `hasQuiz`/`hasAssignment` remain for the UI badges); the `/api/lessons/[id]` 403 no longer echoes the requirement row; the dashboard `continueLesson.videoUrl` and the homework list are gated.
  - Deadlock fixed: no student-side homework submission endpoint existed (`POST /api/students/me/homework` → 405), so `assignmentDone` — one of the three unlock gates — was unreachable and any lesson with an assignment locked the course forever. Added `POST` (session-derived `studentId`, gated, `SUBMITTED`/`LATE`, graded submissions immutable, upsert on the unique pair) plus a minimal in-place submit form. Grading stays teacher-only.
  - Unlock rule reused unchanged: `unlocked[N+1] = video(≥95%) AND assignment AND quiz`, strictly sequential, missing components not required. No second progression system, no schema change, no new dependency.
  - Uniform denial helper `denyProgression()` in `src/lib/api.ts`: 404 for unknown/unverifiable resources (existence never confirmed), 403 + `code` otherwise, never a status row.
  - Phase 3 carry-over closed: the `process.exit(1) is not supported in the Edge Runtime` build warning is gone. Cause: `src/proxy.ts` runs on the Edge runtime so `instrumentation.ts` is bundled for both runtimes, and Next warns on any `process.<member>` call except `process.env`. The call now goes through `globalThis.process`; the production fail-fast is unchanged and was re-verified (build aborts, and the server exits 1, when `SECURITY_HASH_SECRET` is missing). Build now emits **0 warnings**.
  - **Follow-up (progression universe, 2026-09-08):** `getCourseSessionProgress()` discovered lessons only through `topic → unit → part → course`, so official lessons using the canonical `Lesson.unitId` (`topicId = null`) were absent from the progression universe — an *invisible* session, which is worse than a locked one, because `canAccessLesson` answered 404 and the student could never progress past it. The universe now matches on **both** chains (`OR: [{ unit: { part: { courseId } } }, { topic: { unit: { part: { courseId } } } }]`), ordering moved into `orderCourseLessons()` as a deterministic total order (Part → Unit → Topic → Lesson → id, unit-linked lessons first inside a unit), and `/api/courses/[slug]` serves canonical `unit.lessons` alongside legacy `unit.topics[].lessons`. One system, no schema change, no migration, no fake `Topic` rows.
  - Tests: 579 baseline + 144 new (`tests/session-progression.test.js`) = 723 passing; typecheck 0 errors; lint unchanged (44 pre-existing, byte-identical finding list); production build PASS with **0 warnings**; 87 live API security assertions pass against the standalone production server (baseline: 8 failures), covering the canonical curriculum, the legacy topic-linked curriculum and cross-course isolation.
- **Question Bank & Session Quiz (Phase 5 of current cycle): completed (2026-09-08). Baseline `0007808`. See `docs/PHASE_5_QUESTION_BANK_SESSION_QUIZ.md`.**
  - **Attempt question-set persistence (critical):** the set was re-derived live from the quiz's questions on every request, so any bank edit mid-attempt mutated an open attempt (verified live: an attempt grew 3→4 questions after an admin add). The set is now FROZEN into the attempt as unanswered `QuizAnswer` rows at `/start` (`src/lib/session-quiz.ts`); `GET` serves the open attempt's persisted set (refresh / navigation / logout-login byte-identical — verified live), `/submit` grades exactly those rows and updates them in place. Questions added later only join future attempts; pre-Phase-5 in-flight attempts adopt the live set once (documented upgrade path).
  - **Canonical-curriculum blind spots closed (Phase 4 bug class, quiz surface):** `/api/lessons/[id]` returned `part/unit/topic/course: null` for canonical `unitId`-linked lessons (the student lesson view crashed on `data.course.slug`), `/api/quizzes/[id]` returned `courseSlug: null`, the teacher quiz list/creation used the topic chain only, the mock-exam pool excluded canonical lessons' questions, and lesson prev/next ignored canonical siblings. All now resolve through BOTH chains with the canonical link first; prev/next reuse `orderCourseLessons()` (one ordering definition).
  - **Multi-quiz session deadlock fixed:** the engine requires EVERY quiz of a lesson to be attempted, but the lesson API/UI exposed only `quizzes[0]` — a session with 2+ quizzes could never complete. `/api/lessons/[id]` now returns `quizzes` (all) alongside the back-compat `quiz` (first), and the lesson view renders a card per quiz.
  - Grading hardened: server-side-only grader (`gradeAttemptQuestionSet`) over the attempt's own set; client `score/percentage/isCorrect/marks/passed/studentId` never read; `selected` sanitised to a bounded option index; answers for foreign questions ignored; finished attempts immutable (repeat submit = fresh retake).
  - Teacher scope fix: `/api/admin/ai-generate-quiz` no longer lets a TEACHER generate questions into any course's lesson (course-ownership check, canonical-chain aware; admins unrestricted).
  - One focused migration (`20260908120000_phase5_quiz_answer_unique`): `@@unique([attemptId, questionId])` + questionId index on `QuizAnswer`, with a dedup guard that only removes provably duplicate pairs (keeps newest). No other schema change; attempt immutability uses the existing relation, not new columns.
  - Mock Exams untouched except the minimal pool fix (canonical lessons included); `ExamAttempt` stays fully separate from `QuizAttempt` (asserted).
  - Tests: 723 baseline + 70 new (`tests/session-quiz.test.js`) = 793 passing; typecheck 0 errors; lint baseline unchanged (78 pre-existing problems, no new findings); production build PASS with 0 warnings; **46/46 live API/security assertions pass against the standalone production server** (unauthenticated, not-enrolled, locked-session, leak-prevention, freeze-stability, tampering, cross-student isolation, mixed-curriculum, teacher surface, mock-exam separation, malformed input).
- **Quiz Results & Teacher Analytics (Phase 6 of current cycle): completed (2026-09-08). Baseline `66c2967`. See `docs/PHASE_6_QUIZ_RESULTS_TEACHER_ANALYTICS.md`.** Audit + hardening of the Phase 5 result flow. No Phase 5 grading redesign, no Phase 4 progression change, no Mock Exam change, **no database/schema/migration change**.
  - **Teacher analytics now aggregate FINISHED attempts only.** `GET /api/teacher/analytics` and `GET /api/teacher/quizzes` previously averaged every `QuizAttempt` including open (ungraded, stored `percentage` still the pre-submit default) attempts, which deflated quiz averages and pass rates. Both routes now filter `finishedAt != null`; per-quiz metrics use the new deterministic helpers in `src/lib/quiz-analytics.ts`.
  - **Cross-course teacher scope leak closed.** `GET /api/teacher/analytics` aggregated a student's whole attempt history regardless of course, so historical attempts from a course taught by a *different* teacher could leak into this teacher's numbers. It now resolves the quizzes of the teacher's own courses through BOTH curriculum chains and drops any attempt outside that authorized set (`attemptInQuizScope`).
  - **New pure analytics engine `src/lib/quiz-analytics.ts`** (DB-free, side-effect-free): finished-attempt summarising (attempt-weighted averages/pass rate over finished attempts, participants, best/latest), difficulty breakdown (EASY/MEDIUM/HARD from persisted answers), question performance + weak-question ranking. Surfaced per quiz by `GET /api/teacher/quizzes` (`passRate`, `difficulty`, `weakQuestions`). Aggregation rules are explicit and documented in the module header.
  - Authoritative source-of-truth, result immutability, answer-leak rules, Mock Exam isolation and Phase 4 progression were audited and verified unchanged.
  - Tests: 793 baseline + 44 new (`tests/quiz-analytics.test.js`, behavioural + route invariants) = 837 passing; new pure module type-safe under `tsc --strict`; changed files lint-clean. Full `typecheck`/`build`/live-DB verification could not be re-run here because `prisma generate` needs `binaries.prisma.sh`, which is unreachable from this sandbox (environment limitation; see the Phase 6 doc).
  - Student-performance / session / course analytics dashboards and per-attempt history review are not implemented and are documented as deferred.
- **Parent Dashboard Integration (Phase 7 of current cycle): completed (2026-09-08). Baseline `ae6d16f`. See `docs/PHASE_7_PARENT_DASHBOARD_INTEGRATION.md`.** Audit + hardening of the Parent → Student flow. No Phase 4/5/6 redesign, **no database/schema/migration change**, no dependency change.
  - **Parent content-scope leak closed.** `/api/courses/[slug]`, `/api/lessons/[id]` and `/api/quizzes/[id]` gated students but let any authenticated parent open ANY course/lesson/quiz (video/PDF URLs, quiz identities, correct answers). All three now enforce linkage scope via the new `src/lib/parent-access.ts` (≥1 linked child enrolled, same `getEnrollment` rule as students): foreign course → 403 `NOT_ENROLLED`, foreign lesson/quiz → 404. Staff previews and student gates unchanged.
  - **Link enumeration oracle closed.** Wrong-code vs wrong-phone failures returned distinct 404 messages, confirming guessed (national ID + code) pairs. Both branches (link endpoint + parent registration) now return identical messages.
  - **Parent quiz stats now finished-only** (dashboard, analytics, topic stats) over the whole finished set — the old `take: 20` cap corrupted counts past 20 attempts and open attempts deflated averages (Phase 6 rule, previously teacher-only).
  - **New read-only blocks:** `mockExams` (finished `ExamAttempt`s, strictly separate from session quizzes) and `sessionProgress` (completed/current/locked from the unmodified Phase 4 engine). Attendance unified to PRESENT+LATE; analytics/weekly completion is course-based; homework honors real `maxMarks`; unpublished lessons excluded from denominators.
  - **Multi-child fixes:** the monthly report rendered `children[0]` regardless of selection (now `selectReportChild`, unit-tested); analytics opens on the dashboard-selected child.
  - Tests: 837 baseline + 112 new (`tests/parent-dashboard-isolation.test.js`, real handlers + mock Prisma incl. the full 401/403/404 matrix and read-only tracking) = 949 passing; typecheck byte-identical to baseline (22 pre-existing missing-generated-client errors); lint per-file identical (78 pre-existing). `prisma generate`/`next build`/live-server verification blocked in this sandbox (binaries.prisma.sh unreachable — same as Phase 6); must be re-run where engines are reachable.
  - Known limitations: legacy-chain denominators (shared with the student dashboard; engine-derived session block covers canonical lessons); Phase 1 in-scope parent answer visibility kept; no unlink endpoint; no parent quiz-review UI.
- **Mock Exam Cleanup & Integration (Phase 8 of current cycle): completed (2026-09-08). Baseline `99b7331`. See `docs/PHASE_8_MOCK_EXAM_CLEANUP_INTEGRATION.md`.** Audit + hardening of the existing Mock Exam system. No Phase 4/5/6/7 redesign, **no database/schema/migration change**, no dependency change, no second mock system.
  - **CRITICAL fix: GET `/api/exams/mock` shipped the answer key.** Every served question carried `correctIndex` + `explanation`, so any student could read all correct answers from the network tab before answering. Drafts now carry prompts + shuffled options only; correctness is revealed solely by the new POST `review` payload (`correctText`/`explanation`/server `isCorrect`) after submit.
  - **Grading-correctness fix: the client sent shuffled option INDEXes while the server grades selected TEXT** (display order is shuffled per request), making mock scores coincidental. The client now sends the selected option text, matching the documented server contract; forged `isCorrect`/`marks` remain server-overridden.
  - **FIXED mode was random:** pins were shuffled, difficulty-filtered, and sliced at serve time. FIXED now serves the pinned set as-is in pinned `order` (pins rule over `questionCount`/`difficulty`); zero surviving pins returns `exam: null`, never foreign padding.
  - **Student integration added:** `GET /api/students/me/mock-exams` lists published exams of the student's bank + course with her own attempt count/best (the `mockExamId` plumbing existed but was unreachable from any UI); the setup phase renders assigned exams with per-exam Start. Serve-time gates: unpublished → 404, foreign bank → 403, foreign course → 404.
  - Hardening: POST enrollment gate (unenrolled craft-submits blocked), `count` clamp 1–100, `examType`/`difficulty` whitelists, 200-row submission cap, non-string ids graded-as-unknown, linked attempts inherit exam `durationMin`/`passMark`, attribution requires published + bank + course match.
  - Admin: pool guards + reported sizes now count `Question + ExamQuestion` (serving draws from both; satisfiable exams were rejected); `schoolType` switch sweeps stale pins from BOTH link types (shared survive); `courseId` validated at create; delete still detaches attempts (history survives).
  - Verified invariants: submit-atomic attempts (GET creates nothing; retakes append); display-only timing (documented contract); teacher analytics mock-free (no teacher surface, kept as-is); parent `mockExams` block finished-only + child-scoped + read-only; zero cross-writes either direction between `QuizAttempt`/`QuizAnswer`/`LessonProgress` and `ExamAttempt`.
  - Tests: 949 baseline + 135 new (`tests/mock-exam-phase8.test.js`, real handlers + mock Prisma, same offline pattern as Phase 7) + 1 auto-added security-hardening route-enumeration assertion = 1085 passing; typecheck identical to baseline (22 pre-existing missing-generated-client errors, zero in Phase 8 files); lint identical (78 pre-existing). `prisma generate`/`next build`/live-server verification blocked in this sandbox (binaries.prisma.sh unreachable — same as Phases 6/7); must be re-run where engines are reachable.
  - Known limitations: refresh-before-submit discards the draft (documented contract); FIXED pins auto-sampled at create with no curation UI; bank-global guards vs course-scoped serving; no student history endpoint; no teacher mock reporting; double-submit appends two rows.
- **Calendar / I18n (Phase 9 of current cycle): completed (2026-09-08). Baseline `e08f9e6`. See `docs/PHASE_9_CALENDAR_I18N.md`.** Root-cause fix + hardening of Calendar and bilingual UI. No Phase 4/5/6/7/8 redesign, **no database/schema/migration change**, no dependency change, no second i18n or calendar system.
- **Kodgy AI Assistant (Phase 10 of current cycle): completed (2026-09-08). Baseline `12e4e63`. See `docs/PHASE_10_KODGY_ASSISTANT.md`.** Rebuilt the old external-AI Kodgy into a client-side **scripted** assistant with a code-first animated robot + flame/energy: `src/lib/kodgy/response-engine.ts` (deterministic bilingual matcher, platform + education topics, graceful fallback), `src/lib/kodgy/position.ts` (Arabic LEFT / English RIGHT, drag clamping), `src/components/kodgy/*` (robot, chat panel, controller). Removed `/api/ai/chat` + old `ai-assistant.tsx` — **no external AI, no `/api/ai`, no secrets, no fetch**. Draggable (mouse+touch, viewport-clamped, per-locale localStorage), responsive (1440/834/390), light/dark, RTL/LTR, reduced-motion. **No database/schema/migration change.** Tests: 231 new offline assertions + **142 real-browser assertions** (Chromium via `@sparticuz/chromium`) all passing; all Phase 4–9 + security suites green (1,752 total); typecheck 0 errors in Phase 10 files (22 pre-existing Prisma-generated-client errors, sandbox cannot reach `binaries.prisma.sh`); lint 0 findings in Phase 10 files; `next build` still blocked by the Prisma engine download (same as Phases 6–9).
  - **Root cause of `student.198`…`student.204` raw-key leak:** the Study Scheduler weekday header row rendered the key strings directly (`{d}`) instead of calling `tr(d)`. The dictionary entries already existed with correct AR/EN values; month headers and the selected-day title already translated correctly. Fix: headers call `tr(key)`; constants renamed `DAY_NAME_KEYS` / `MONTH_NAME_KEYS` to make the contract obvious.
  - **Missing-key fallback hardened** in `src/lib/i18n-core.ts`: `translate()` never returns a dotted internal key (`student.999` → `""`); empty locale strings fall through to the other locale; human non-key strings still passthrough. New helpers: `looksLikeDictKey`, `hasDictKey`, `dictSize`.
  - **Shell nav bare-English labels** (`"Study Plan"`, `"Dashboard"`, `"Attendance"`, …) replaced with dedicated `shell.027`…`shell.038` keys so Arabic mode no longer shows English chrome for those items; Support button localized.
  - **Scheduler UX:** localized title (`student.243`), prev/next/today a11y labels, month nav forced `dir="ltr"` (time progression stable under RTL), dark-mode-aware selected/today styles, mobile touch targets, `role="grid"` + localized day `aria-label`s, locale-independent day identity (`localDayKey`).
  - **Server date localization:** parent weekly-report and certificate completion date use `serverLocale()` + `fmtDate` instead of hardcoded `ar-EG`. shadcn `CalendarDayButton` `data-day` is ISO `YYYY-MM-DD` (locale-independent).
  - Tests: prior offline baseline + **440 new** (`tests/calendar-i18n-phase9.test.js`) all passing; Phase 4/5/6/7/8 suites green; typecheck identical class of pre-existing missing-generated-client errors (zero in Phase 9 files); lint **improved** vs prior baseline (76 errors / 1 warning; Phase 9 files clean). `prisma generate`/`next build`/live-server verification blocked in this sandbox (binaries.prisma.sh unreachable — same as Phases 6/7/8); must be re-run where engines are reachable.
  - Known limitations: DB-seed residue still Arabic under EN (pre-existing); full Playwright i18n audit deferred until engines available; week-start remains Sun (product choice).
- **Official Curriculum Reconciliation (Phase 11 of current cycle): completed (2026-09-09). Baseline `adfdb67`. See `docs/PHASE_11_OFFICIAL_CURRICULUM_RECONCILIATION.md`.** _(Post-merge defect fixed in Phase 12 — see below: the reconciler ordered `Part`/`Unit` by a `createdAt` column those models do not have, which the suite's `orderBy`-ignoring mock could not catch.)_ Reconciled the live synthetic R1 curriculum (36 lessons) to the official R2 curriculum from `docs/curriculum/knowledge-model.json` (2 parts, 7 units, 23 lessons `1-1`…`7-3`), idempotently and without destroying user history. **No database/schema/migration change** (schema already carried `unitId`/`officialCode`/`CurriculumStatus`), no dependency change, no Phase 12+ work.
  - **New reconciler** `src/lib/official-curriculum.ts` (+ CLI `scripts/reconcile-curriculum.ts`): strict fail-closed model loader (exact 2/7/23 + code set); course upsert by unique slug; positional part/unit adoption (legacy P2 units 1,2,3 adopted in place, renumbered to global 5,6,7 — no duplicates, no `Topic` rows created); lesson upsert by unique `officialCode` writing canonical fields only (media/topic links/relations preserved); archive sweep flips code-less in-course lessons to `ARCHIVED` via one `updateMany` (no deletes); post-state assertions + warnings for unknown codes/extras. Second run performs zero writes; crash-safe by re-run.
  - **Progression universe is exactly the 23 official lessons:** new shared predicates (`EXCLUDE_ARCHIVED_LESSON`, `lessonCourseChainOr/Courses`) applied to the engine universe, course tree (+ empty-topic filter), lesson prev/next, student dashboard/homework/certificate, parent dashboard/analytics/weekly (both fraction sides universe-scoped, no >100%), and video progress (inline, no import cycle). Teacher/admin scopes are dual-chain with NO exclusion (grading + full-catalogue management); admin counts add deduped `lessonsCount` + `activeLessonsCount`; AI quiz generation 410s archived targets.
  - **Seed retired:** `{action:"seed"}` → 410 Gone (`api.226`); new `{action:"reconcile-official"}` returns the report; CoursesView reconciles with report toast, active/total tiles, `unit.lessons` tree with Archived badges, dual-chain AI picker; `scripts/seed.ts` §4 + `scripts/seed-curriculum.ts` repointed at the reconciler; R1 modules deprecated with a runtime `[RETIRED]` trip-wire (still pinned by `tests/seed-idempotency.test.js`).
  - Tests: **51 new** (`tests/curriculum-reconciliation-phase11.test.js`, offline mock-Prisma incl. a real `where` evaluator) + session-progression §22 strengthened to assert the full fetch shape with exclusion plus §11b/§20 archived-universe guards (144→151); **1,810 offline assertions across 16 suites, 0 failures**. Typecheck/lint/build: byte-identical to baseline (22 pre-existing stub-client errors; 6 pre-existing tsx hook errors); Turbopack compile PASS; `prisma generate`/`migrate status`/full build blocked in this sandbox (`binaries.prisma.sh` unreachable — same as Phases 6–10); must be re-run where engines are reachable, then reconcile once against a backup.
- **Track Architecture & Cross-Track Enforcement (Phase 12 of current cycle): implemented and verified (2026-09-09). Baseline `66f56f5`. See `docs/PHASE_12_TRACK_ARCHITECTURE.md`. PR open — deliberately NOT merged (awaiting pre-merge review).** Makes `ARABIC student → SHARED + ARABIC only` and `LANGUAGE student → SHARED + LANGUAGE only` **structurally true server-side and fail-closed**, over the one existing shared course `programming-ai-2nd-sec`. No second course, no second progression engine, no second enrollment gate, no new index.
  - **Phase 11 defect fixed first (mandatory prerequisite):** `reconcileOfficialCurriculum` ordered `Part`/`Unit` by `createdAt`, a column neither model has → `PrismaClientValidationError: Unknown argument 'createdAt'`. Fixed with a shared deterministic tie-breaker `PART_UNIT_ORDER_BY = [{order:"asc"},{id:"asc"}]` on both queries. **No `createdAt` was added to `Part`/`Unit` and no migration was created to satisfy the query.** The Phase 11 suite's mock ignored `orderBy` (and even synthesised `createdAt`), which is why 51 green assertions missed it — the mock now validates `orderBy` keys against fields parsed from `prisma/schema.prisma` and mirrors the Prisma error. Mutation-verified: re-introducing `createdAt` crashes the suite; the fix restores 51/0.
  - **Schema (additive):** new `enum TrackScope { SHARED ARABIC LANGUAGE }`; `trackScope TrackScope @default(SHARED)` on `Lesson`, `Quiz`, `Homework`; `Student.schoolType` constrained `String?` → `SchoolType?` and **kept nullable on purpose** ("unspecified" is a real state; `NULL` fails closed to SHARED-only). Prisma `Track`/`Enrollment` formally documented as DEPRECATED / DEAD SCHEMA (zero references in `src/`); `Group → Course` via `getEnrollment` stays the sole access gate. Session videos get **no new column** — track derives from the non-nullable `Batch.schoolType`.
  - **Migration** `20260909120000_phase12_track_architecture`: normalisation UPDATEs **before** the DDL (ARABIC/LANGUAGE alias mapping; anything else → `NULL`, never a guess), then the three `trackScope` columns as `TEXT NOT NULL DEFAULT 'SHARED'` via Prisma's SQLite table rebuilds with all three row-copy statements. No data deleted, no historical migration edited. `migrate status` → "5 migrations found / up to date!". **Critical finding: a SQLite enum emits no CHECK constraint — `String?`→`SchoolType?` produces zero DDL — so the normalisation is the real safety step.**
  - **One source of truth:** new `src/lib/track-scope.ts` holds the whole matrix (`canAccessTrackScope`, `eligibleTrackScopes`, `trackScopeWhere`, `trackScopeInWhere`, `videoTrackFilter`, `eligibleQuestionFilter`, `isQuestionEligible`, `resolveQuestionSchoolType`, `parseQuestionSchoolTypeInput`). Unknown on **either** side is refused rather than mapped to SHARED.
  - **Enforcement:** the progression universe in `getCourseSessionProgress` is track-filtered, so an ineligible lesson never enters `sessions`, never becomes an unlock target and is never the continuation pointer; `canAccessLesson` adds an explicit gate and returns the **same non-oracle** `LESSON_NOT_FOUND` as a nonexistent id; `canAccessQuiz`/`canAccessHomework` add `gateTrackedResource`. `session-quiz` gained a `schoolType` parameter on `seedAttemptQuestions` (selection), `loadAttemptQuestionSet` (serving — narrows even a pre-Phase-12 frozen set), `loadQuizQuestionSet` (retake) and `gradeAttemptQuestionSet` (an ineligible question contributes to neither `score` nor `totalMarks`). A completeness audit of all 80 route files then closed five further surfaces — denominators and embedded lists, not obvious endpoints: `students/me/dashboard` (progress denominator **and** an embedded homework list that was an outright cross-track content leak), `students/me/certificate` (80% threshold divided by unreachable lessons), `parents/me/dashboard` (per-child universe + homework), `parents/me/analytics` and `parents/me/weekly-report` (union of the linked children's tracks). Also gated: `/api/courses/[slug]` (both curriculum chains), `/api/lessons/[id]` (quizzes, homework, prev/next), `/api/quizzes/[id]` + `/start` + `/submit`, `/api/students/me/homework`, `/api/students/me/session-videos`, `/api/media/[id]`.
  - **Batch healing:** new `reconcileStudentBatch()` — deterministic, idempotent (`changed:false` on re-run), never assigns without a recognised school type, prefers an ACTIVE course-specific batch then an ACTIVE course-less one, never another course's, and **clears** a wrong-track/wrong-course batch rather than keeping it (`OK|ASSIGNED|REPLACED|CLEARED|NO_SCHOOL_TYPE|NO_BATCH|NO_ENROLLMENT`). Wired into all five write paths (register, admin create, admin PATCH on school-type **or** course change, self-enroll, batch create) + `attachUnassignedStudentsToBatch`. `requireSchoolType` validates registration/admin writes, accepting the documented legacy aliases and rejecting anything else.
  - **Teacher tagging precedence:** explicit payload value wins (explicit SHARED stored as `NULL`) → else inherit the owning **quiz's** `trackScope` (a SHARED lesson may host both an ARABIC and a LANGUAGE quiz) → else `NULL`. Invalid values are rejected (400, `api.228`/`api.229`), never downgraded.
  - **Parents** follow the union of their LINKED + ENROLLED children's school types (`getParentTrackScopes`/`isParentAllowedTrackScope`, bare 404 on refusal). **Track-safe analytics** added (`trackBucketOf`, `partitionByTrack`, `summarizeFinishedAttemptsByTrack`): a report buckets an unknown tag as SHARED rather than dropping the row, and every bucket re-uses `summarizeFinishedAttempts` so a split can never disagree with its total.
  - **Security boundary asserted in tests:** no route derives `trackScope`/`schoolType` from `body`/`params`/`searchParams`, and `track-scope.ts` never reads a locale or `headers()`.
  - Tests: **291 new** (`tests/track-architecture-phase12.test.js`: pure matrix contract, behavioural run of the real engines against a Phase-12-schema mock, route source invariants + security boundary, schema/migration assertions), **mutation-verified** (neutering `canAccessTrackScope` → 25 failures; removing the universe filter → 6). Existing suites repaired, **never weakened** — fixtures updated to carry the schema's real `SHARED` default, three source-invariant regexes **tightened**; baseline counts identical before/after. **2,092 offline assertions across 17 suites, 0 failures.** `npm run typecheck` → 0 errors. Plus a real-database end-to-end run (real Prisma client, real SQLite, unmodified source): reconciler idempotent across two runs, 2/7/23 curriculum intact with no duplicate `officialCode`, three `trackScope` columns `NOT NULL DEFAULT 'SHARED'`, 0 out-of-enum `schoolType`, backfill re-run changing 0 rows, and the full cross-track matrix through the real gating functions — 37/0.
  - Known limitation: SQLite stores both enums as plain `TEXT` with no CHECK constraint, so the Prisma client is the only gate for out-of-enum values written by raw SQL; the reconciler fails closed (`NO_SCHOOL_TYPE`) rather than guessing.
- Next planned phase: only after explicit approval.

## Architecture summary
Next.js application with Prisma and SQLite. The database remains SQLite, while the schema uses portable relational concepts and stable IDs. Existing operational grouping (`Group`) remains separate from explicit student access (`Enrollment`).

## Phase 4 progression decisions
- `src/lib/session-progress.ts` stays the single source of truth. Phase 4 added *resource* gates (`canAccessQuiz`, `canAccessHomework`, `getUnlockedLessonIds`) that all resolve to the owning lesson and re-use `canAccessLesson` — there is still exactly one definition of "may this student open this session".
- **The progression universe follows the canonical curriculum.** A lesson belongs to a course through `Lesson.unitId` (Course → Part → Unit → Lesson) **or** the legacy `Lesson.topicId` chain; the canonical link wins when both exist (`resolveLessonCourseId`). Ordering is a deterministic total order computed in `orderCourseLessons()`, not in SQL, because `Topic` is nullable. Official lessons never need a placeholder `Topic`, and the course tree presents a lesson exactly where the engine enforces it.
- A locked session is described by title/number/duration plus `hasQuiz`/`hasAssignment` presence booleans. Media URLs, material URLs, quiz/assignment identities, requirement breakdowns and summaries are server-side only; hiding them in the UI was never protection.
- Denials are uniform (`denyProgression`): 404 when a resource is unknown or not attached to a verifiable course (existence is never confirmed), 403 + machine-readable `code` otherwise, and never a requirement/status row.
- Assignment completion is still "a `HomeworkSubmission` exists with `submittedAt`". Phase 4 only made that row *reachable* by the student; it added no grading behaviour.
- Batch `SessionVideo`/`SessionVideoView` remain a separate, admin-published delivery channel gated by `batchId + isPublished`. They are deliberately **not** wired into lesson gating — doing so would create the second progression system this phase forbids.
- `LiveSession.meetingUrl` stays a group-calendar item, not a progression reward.
- `Lesson.isLocked` remains inert legacy metadata (the curriculum seed sets it for every non-first lesson, so enforcing it would deadlock the course).
- `Lesson.unit` / `Lesson.topic` are `onDelete: SetNull`, so deleting a Part/Unit/Topic orphans its lessons instead of removing them (17 such rows already exist in the seeded database). An orphan lesson belongs to no course and is correctly invisible to progression; cleaning those rows up is a data task.

## Phase 5 quiz decisions
- The Session Quiz "configuration" is the existing `Quiz` → `Question` ownership (plus `passMark`/`timeLimit`/`order`). No selection/randomization fields were added to `Quiz` — per-student random sampling stays a Mock Exam capability.
- Attempt immutability is implemented **by relation, not by duplication**: the attempt's question set is persisted as `QuizAnswer` rows at `/start` (`src/lib/session-quiz.ts` is the single selection + grading module). No question content is copied; grading always reads the live authoritative answer key of the frozen question ids.
- A question added to a quiz after an attempt started can never join that attempt (future attempts only). A question EDIT keeps its place in open attempts and grades against the current key; a question DELETE cascades its answer rows away (pre-existing schema semantics).
- `@@unique([attemptId, questionId])` on `QuizAnswer` makes "one answer per question per attempt" a database invariant (same guarantee style as `HomeworkSubmission`).
- Attempts opened before the Phase 5 deploy (no persisted rows) adopt the live quiz questions until submit — a one-time, backwards-compatible upgrade path, never reachable by new attempts.
- Session Quiz (`QuizAttempt`/`QuizAnswer`) and Mock Exam (`ExamAttempt`, JSON snapshot) share the Question Bank and nothing else.
- Quiz completion remains "a finished `QuizAttempt` exists for every quiz of the lesson" — the Phase 4 engine is untouched; Phase 5 only made every quiz of a session reachable (`quizzes` array) so the requirement cannot deadlock.

## Phase 6 analytics decisions
- Teacher quiz analytics are computed over **finished attempts only**, from the server-graded stored row. `src/lib/quiz-analytics.ts` is the single, pure, deterministic aggregation module (DB-free and side-effect-free so reporting can never mutate assessment state).
- Averages and pass rates are **attempt-weighted**: every finished attempt is one data point, so retakes each count. Unfinished and Mock Exam attempts are never included. Deleted questions disappear from analytics (their answer rows cascade); edited questions keep historical correctness under current difficulty.
- Teacher analytics are server-side course-scoped: attempts are restricted to quizzes of the teacher's own courses (both curriculum chains) before aggregation (`attemptInQuizScope`), so a student's cross-course history cannot leak into a teacher they no longer take that course with.
- No schema change; Phase 4 progression and Mock Exam separation are untouched.

## Phase 8 mock exam decisions
- The exam draft carries NO answer key. Prompts + shuffled options are served; `correctText`/`explanation`/correctness are revealed only by the POST-submit `review`. Key disclosure is treated as critical regardless of server-side grading, because grading cannot fix a leaked key.
- The client answers with selected option TEXT, never an index: display order is shuffled per request, so positions are meaningless to the grader. The server accepts the stored index as a back-compat fallback and always recomputes correctness/marks from the DB key.
- Attempts are submit-atomic: GET mints a stateless draft, POST appends one finished `ExamAttempt` with an immutable JSON snapshot. There is deliberately no server-side in-progress attempt, no timeout enforcement, and no overwrite — retakes are history rows.
- FIXED means pinned-set-as-is (pinned `order`, no filter/shuffle/slice); RANDOM means re-sampled per request under the bank + course + difficulty rules. Both are served through the same bank filter as grading, so selection and scoring can never disagree on the bank.
- Attribution (`mockExamId`) requires published + same bank + (when bound) same course; anything else degrades to an unlinked practice attempt instead of failing the submit. `courseId: null` means "all courses of this bank".
- Mock exams share the Question Bank and nothing else: no `QuizAttempt`/`QuizAnswer`/`LessonProgress`/progression writes exist in the mock flow, and no `ExamAttempt`/`MockExam` reference exists in session-quiz, teacher-analytics, or progression code. The parent `mockExams` block is a read-only finished-attempts projection, never mixed into quiz stats.

## Authoritative curriculum
The four committed PDFs and the Phase 2 knowledge model are authoritative. The canonical hierarchy is Track → Course → Part → Unit → Lesson. The old Topic layer is retained only as a nullable legacy compatibility wrapper; official lessons use `Lesson.unitId`, `officialCode`, and `curriculumStatus=OFFICIAL`.

## Important architectural decisions
- Added explicit `Track`, `Enrollment`, and `Material` entities.
- Existing `Question` remains the central question record; `Quiz`, `ExamQuestion`, and mock links are assessment projections/legacy compatibility, not a second bank.
- `MediaAsset` is the storage abstraction. `SessionVideo` is the single video publication concept and `SessionVideoView` is student progress.
- `LiveSession`, `Homework`/submission, and `QuizAttempt` remain the existing operational domains and are lesson/session-linked.
- No Phase 4 runtime, curriculum reseed, randomization, upload UI, or progression behavior was implemented.

## Security decisions
This phase adds no authentication behavior. Material stores a storage key/media reference rather than trusting filenames; existing token hashing and audit structures remain unchanged. Foreign keys and restrictive media deletion protect historical references.

## Technical debt and known issues
The repository has no Phase 1 report at the requested path. Existing seed data still contains 36 synthetic lessons and must not be silently mapped. A later approved data migration must seed 23 official lessons using deterministic `officialCode` values and archive/unmap the remainder. Existing `Topic`, `Homework`, and `ExamQuestion` names require future compatibility cleanup, but were not destructively renamed.

## Dependencies and migration risks
Prisma 6.11.1, SQLite, and the existing migration history are dependencies. The Phase 3 migration is additive and does not backfill or delete data. SQLite's nullable unique semantics are relied on for optional legacy lesson codes; PostgreSQL migration should preserve explicit nullability and named indexes.

## Implementation status
Schema additions and migration are complete for the foundation. Documentation is complete; Phase 3 PR #14 is open and awaiting approval.

## Deployment Readiness Audit fixes (2026-09-07)
- **CRITICAL (C1):** Registration endpoint no longer allows self-registration as ADMIN/TEACHER.
- **HIGH (H1):** Quiz answers/explanations withheld from students until they have at least one finished attempt.
- **HIGH (H2):** Legacy email-based parent-student linking removed; all linking requires verified path (national ID + student code + parent phone).

## Known risks
- SQLite is not suitable for concurrent production use (M2 — deferred).
- ~~`ignoreBuildErrors: true` suppresses TypeScript errors during build (M1 — deferred).~~ **Resolved (2026-09-07, Phase 2 of current cycle):** the flag was removed; builds type-check for real and all 67 previously hidden errors were fixed.
- ~~`SECURITY_HASH_SECRET` falls back to a hardcoded value if not set (M3 — deferred).~~ **Resolved (2026-09-07, Phase 3):** required in production; build and startup fail fast.
- ~~No `middleware.ts` for edge-level route protection (M4 — deferred).~~ **Resolved (2026-09-07, Phase 3):** `src/proxy.ts` guards protected API namespaces (defense-in-depth only).
- `xlsx@0.18.5` has unfixable-on-npm advisories; exposure limited to the admin-only payments import (Phase 3 — documented, deferred).
