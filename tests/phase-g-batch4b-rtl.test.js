const fs=require('fs'),path=require('path');const s=fs.readFileSync(path.join(__dirname,'..','src/components/teacher/teacher-authoring.tsx'),'utf8');let p=0,f=0;const ok=(x,m)=>x?p++:(f++,console.error('FAIL',m));ok(s.includes('function DeadlineField'),'shared deadline field');ok(s.includes('type="time"'),'native time control');ok(s.includes('dir="ltr"'),'time input remains legible in RTL');ok(s.includes('flex-wrap items-stretch gap-2'),'responsive non-clipping row');ok(s.includes('max-h-[85vh] overflow-y-auto'),'scroll-safe dialog');ok(s.includes('toLocalInput(homework?.deadline)'),'edit preserves saved deadline');ok(s.includes('new Date(deadline).toISOString()'),'stored timestamp behavior preserved');console.log(`phase-g-batch4b-rtl: ${p} passed, ${f} failed`);process.exitCode=f?1:0;
