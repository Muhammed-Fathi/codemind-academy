# تقرير ما بعد الإطلاق — CodeMind Academy
**الفرع:** `arena/01a0af17-codemind-academy` · **التاريخ:** 17 سبتمبر 2026 · **الإصدار:** v2 (بعد اعتماد المراجعة + إصلاح اتساق أسعار الـ landing) · **الحالة:** مكتمل للمراجعة النهائية (بدون PR)

---

## 1) الملخص التنفيذي
تم تنفيذ 5 متطلبات بعد الإطلاق مع إصلاح 5 عيوب، كل منها مضبوط بتجارب رجعية حالية (106 ادعاءات جديدة على قاعدة بيانات SQLite حقيقية بالهااندلرز المُجمّعة من الشيفرة الفعلية):

| # | المطلوب | الحالة |
|---|---------|--------|
| 1 | نظام إشعارات كامل لكل الأدوار الأربعة + إشعار ولي الأمر بتعليقات المعلم | ✅ |
| 2 | المعلم يكتب تعليقًا على الطالب، وولي الأمر يستلمه مع إشعار | ✅ |
| 3 | إعادة تصميم قسم التواصل في الفوتر (اسم + رقم متلاصقان) | ✅ |
| 4 | دورة حياة الباقات: الإغلاق ≠ الحذف، الباقة المغلقة تظهر "غير متاحة حاليًا" | ✅ |
| 5 | **أسعار الـ landing من مصدر الحقيقة** (`PricingSection` ← `GET /api/subscription-plans`) | ✅ (إصلاح v2) |
| 6 | تجارب رجعية لكل تعديل + إعادة تشغيل كامل السويتات | ✅ (55/58 — الفشل 3 بسبب بيئة PostgreSQL غير متاحة، موجودة من قبل) |

**لم يُلمس الإنتاج إطلاقًا:** لا هجرات، لا بيانات، لا deploy، لا PR. كل الاختبارات على قاعدة مؤقتة (in-memory).

---

## 2) تشخيص عيب الإشعارات (قبل الإصلاح)
المشكلة لم تكن في التخزين ولا في جرس التنبيه — بل في **طبقة العرض**:

1. `NotificationsBell` في `shell.tsx` كان يوجّه **كل الأدوار غير الأدمن** إلى `student-notifications` فقط.
2. `renderView` في `app-shell.tsx` (Phase26E) كان يرجع dashboard الدور عند أي view مفتاح — فالمعلم كان يسقط على "overview" والأب تجاهل المفتاح تمامًا.
3. النتيجة: **الشارة (badge) تظهر بعدّاد حقيقي، والضغط لا يُظهر شيئًا** — بالضبط ما رآه المعلم وولي الأمر.

**الحل:** لوحة إشعارات مشتركة واحدة + مفاتيح view خاصة بالمعلم/الأب + خريطة جرس حسب الدور + whitelist مشترك.

---

## 3) الإصلاح المعماري (الملفات الجديدة)

### `src/lib/view-roles.ts` (جديد — مصدر الحقيقة الوحيد)
`VIEWS_BY_ROLE` + `PUBLIC_VIEWS` + `isViewForRole(view, role)`. قائمة "أي view يُسمح بدفعه لأي دور" معرّفة **مرة واحدة** وتُستهلك في مكانين كانا يجب أن يتفقوا:
- **app-shell** — استعادة الجلسة: view محفوظ قديم/غير قانوني → رجوع لدار الدور بدل عرض مكون سيُرجع 403.
- **لوحة الإشعارات** — زر "فتح" يظهر فقط إذا كان هدف الـ deep link قانونيًا **لدور المستخدم الحالي** (معلم يستلم إشعار `lesson:<id>` لن يُدفع لرؤية الطالب — الشاشة تموت على `/api/lessons/…` بدور المعلم).

### `src/components/shared/notifications-panel.tsx` (جديد — واجهة واحدة للأدوار الأربعة)
- fetch **مرة واحدة عند الفتح** (الـ view يُفكك عند الخروج → إعادة الدخول تعيد الجلب). لا polling.
- تفضيلات: mark-read متفائلي + mark-all + شارة العدد.
- **الـ deep link عبر `navigateDeepLink` فقط** (ترتيب Phase 16: `setView` أولًا — لأنها تُصفّر `navParam` — ثم `setNavParam`)، مع تحقق `isViewForRole` قبل عرض زر "فتح". أي رابط قديم/موروث/غير قابل للتحليل = **معلومة فقط**: النقر يقرأ الإشعار ولا ينقل لأي مكان.
- `NOTIFICATIONS_CHANGED_EVENT` + `emitNotificationsChanged()` — الجرس يتزامن **فوريًا** بعد أي قراءة (بدون دورة polling).
- حالات تحميل/فراغ/خطأ بأزرار retry.

### `store.ts`
أضيف `teacher-notifications` و`parent-notifications` لمفاتيح `ViewKey`.

### `shell.tsx`
- عنصر "الإشعارات" في قائمة التنقل **للمعلم ولأب** (كانت للطلاب والأدمن فقط).
- `NOTIFICATION_VIEW_BY_ROLE`: STUDENT/TEACHER/PARENT → view الخاص بكل دور، ADMIN → مركز الإشعارات.
- الجرس يمشي على الخريطة + يستمع لحدث التغيّر.

### `app-shell.tsx`
- whitelist المضمّن **حُذف** واستُورد من `view-roles.ts`.
- `renderView` + حالتان: `teacher-notifications` → `TeacherDashboard`، `parent-notifications` → `ParentDashboard` (كلاهما يتفقد الـ view ويعرض اللوحة المشتركة).

### لوحات الطلاب/المعلم/الأب
- **الطالب:** `NotificationsView` غلاف رقيق فوق اللوحة المشتركة (حُذف الـ dead code: `navigateDeepLink/parseDeepLink` المكرران + `Bell` غير المستخدم).
- **المعلم:** early-return للوحة المشتركة قبل توجيه الـ tabs.
- **الأب:** يستلم `view`، وطلب dashboard الرئيسي `enabled: view !== "parent-notifications"` (لا طلبات ثقيلة خلف لوحة خفيفة).
- **الأدمن:** مركز الإشعارات (أداة البث + آخر 30) **أُضيفت إليه بطاقة "إشعاراتي"** باللوحة المشتركة — شارة الجرس للأدمن كانت تعدّ إشعاراته ولا مكان يعرضها؛ الآن الاتساق كامل.

---

## 4) ميزة: تعليقات المعلم → ولي الأمر (مهمة 2)
**قرار معماري مهم:** نموذج `TeacherNote` وقراءة الأب (card في dashboard + آخر 5 تعليقات) **كانا موجودين بالفعل** — المفقود كان **جهة الكتابة**. بُنيت عليها الميزة بدون أي هجرة وبدون نظام موازٍ.

### `POST /api/teacher/notes` ← `src/app/api/teacher/student-notes/route.ts` (جديد)
```
{ studentId, note }  →  201 { note: { id, createdAt }, notifiedParents }
```
سلسلة الأمان (كلها تُنفَّذ فعليًا في الهااندلر المُجمّع):
1. جلسة → دور TEACHER (غيره 403) → `getTeacherProfile`.
2. **Rate limit** بالbucket `notification` الموجود (30/دقيقة) — كل تعليق يفرّخ إشعارات لآباء، نفس قلق التضخيم الذي يحمي به مسلك البث.
3. **الـ scope**: الطالب يجب أن يكون في **مجموعة من مجموعات المعلم** (مُحسوبة server-side). طالب خارج النطاق = **404 نفسه** لطالب غير موجود — لا تسريب وجودية.
4. **Bounding** بالحارس المشترك `boundedText` (3–2000 حرف) — رفض 400 وليس اقتصاصًا صامتًا.
5. إنشاء `TeacherNote` → **auditLog** (`TEACHER_NOTE_CREATE`) → **فرّخ إشعار** لكل أب مرتبط عبر `createNotificationIfAllowed` (يحترم تفضيلات + ساعات الهدوء) — التأثيرات بعد الالتزام ولا تعكس الحفظ.
6. **لا DELETE ولا PATCH** — السجل إضافة فقط (حذفه يترك الأب بلا "من قال هذا؟"، وإعادة إسناد المعلم لا تمحو ما كُتب).

### `GET /api/teacher/student-notes?groupId=&studentId=`
تاريخ تعليقات المعلم على طلاب مجموعاته (محدود بـ 20)، بمسارات رفض متطابقة.

### الواجهة (teacher-dashboard.tsx)
عمود "تعليقات" في جدول الحضور + dialog: textarea بإرشاد حدود + إرسال متفائل + سجل تاريخي. كل النصوص عبر i18n.

### جانب الأب
**يعمل كما هو** (card موجود) — والتجربة تثبت: الأب **يرى** التعليق باسم كاتبه، ويصله إشعار، والأب الآخر **لا يرى** ولا يستلم.

---

## 5) الفوتر — قسم التواصل (مهمة 3)
**كان:** تسمية إنجليزية ثابتة + رقم **يتيم** في سطر منفصل + لا أسماء.
**صار:** لكل صف — تسمية i18n بدور + **سطر واحد** `اسم الشخص · الرقم` (الاسم من `SUPPORT_CONTACTS` في `brand.ts` — مصدر الحقيقة الوحيد، لم يُكرّر أي رقم) + الرقم `tel:` قابل للاتصال + أيقونة واتساب المدمجة. صف الاسم/الرقم `dir="ltr"` (نص لاتيني + أرقام) والقائمة كلها RTL-aware بخصائص منطقية.

---

## 6) دورة حياة الباقات (مهمة 4)
**المبدأ:** الإغلاق (`isActive=false`) ≠ الحذف. الباقة المغلقة **تبقى ظاهرة** للطلاب مرمّزة "غير متاحة حاليًا" بزر معطّل، والتحكم الكامل للأدمن.

### `GET /api/subscription-plans`
لم تعد تصفّي المغلقات — تُرجع كل الباقات **النشطة أولًا** ثم المغلقة بوسم `isActive: false`.
**التحكم ما زال server-side دائمًا:**
- `POST /api/enroll` يرفض الباقة المغلقة عند **الإرسال** (api.276).
- طبقة القرار ترفضها عند **الموافقة** (`PLAN_NOT_FOUND`) — باقة أُغلقت بين الإرسال والموافقة لا تُفعّل أبدًا.
- **صلاحية قائمة** (شراء سابق) لا تتأثر بالإغلاق — الاشتراك هو مصدر الصلاحية، لا حالة الباقة.

### `GET /api/admin/plans/[id]` (جديد)
يرجع الباقة + عدّادات الاعتمادات: اشتراكات نشطة / سجل اشتراكات / مرجعيات مدفوعات + `deleteSafe` — **بنفس منطق حارس الـ DELETE** فلا يخطئ UI أبدًا عن API. **هذا الـ endpoint داخلي للأدمن فقط؛ لا يُعرض في الـ public API ولا في الـ landing.**

### `PricingSection` في الـ landing — أصبح مصدر الحقيقة (إصلاح v2)
**المشكلة:** قسم الأسعار في الصفحة الرئيسية كان يعرض **3 باقات ثابتة** (اسم/سعر/مدة مكتوبة بالكود). أي تعديل من الأدمن (سعر/حالة/مدة) كان يصل لواجهة الحجز (`enroll`) من القاعدة، بينما الـ landing تبقى قديمة — **تضارب سعر مباشر أمام العميل**.

**الحل:** `PricingSection` الآن يجلب من `GET /api/subscription-plans` (نفس المصدر القياسي الذي تقرأ منه `enroll`) — **لم يُنشأ API تسعير ثانٍ**:
- كل بطاقة = باقة حقيقية من القاعدة: `pickAuto(nameAr,name)`، السعر عبر `formatPaymentAmount`، المدة `pay.planDuration`، الوصف من `description`.
- **متاحة:** عرض عادي + CTA تعمل.
- **مغلقة:** تبقى ظاهرة + شارة `plan.023` "غير متاحة حاليًا" + CTA معطّلة (والخادم يرفضها أصلًا).
- **حُذفت:** تختفي تلقائيًا (لم تعد في الـ API).
- **الأرفع سعرًا بين المتاحة = البطاقة المميزة** (التصميم البريميوم محفوظ: نفس الهيكل/الألوان/الشارة).
- **loading** (skeletons) / **فارغ** (`plan.034`) / **فشل API** (`plan.035` + زر retry `plan.036`) — **لا أسعار وهمية احتياطية عند الفشل**.
- **لا يُكشف أي حقل اعتمادات داخلي** — الـ public API يعيد حقول الباقة فقط (مُثبت بتجربة `L23d`).

### واجهة الأدمن (`PlanManagementCard` + `PlanFormDialog`)
إنشاء / تعديل (كل حقول المخطط فقط) / تفعيل / تعطيل / **حذف آمن**: dialog تأكيد يعرض العدّادات، والحذف الفعلي ينفَّذ فقط للباقة **بصفر مراجع**؛ أي مرجع → 409 برسالة تشرح السبب (اشتركات نشطة / سجل) وتوجّه للإغلاق بدل الحذف. **سجل المدفوعات/الاشتراكات لا يُدمَّر أبدًا.**

### عيوب أصلية أُصلحت ضمن نفس المهمة (موجودة قبل عملي):
1. **تكرار رسالة الرفض:** `api.276` في المسودة اليدوية كان `ar` و`en` نسخة واحدة — حُرر النص العربي للأصل.
2. **حقل `retired` غير موجود:** `Plan` object type في `enroll-view.tsx` و`plans` response في `admin-dashboard.tsx` كانا يحملان `retired?: boolean` — حقل غير موجود في المخطط؛ **حُذف** (لم يُخترع بديل، الإغلاق هو الحالة الوحيدة).
3. **PricingSection في الـ landing** بأسماء وأسعار ثابتة — **خارج النطاق** (تسويق ثابت وليس مصدر الحجز)؛ موثق في §15.

---

## 7) التغيّرات على الـ API (عقود)

| endpoint | التغيّر |
|----------|---------|
| `GET /api/subscription-plans` | **تغيّر عقد** (متعمد): + الباقات المغلقة بوسم `isActive:false`، ترتيب: النشطة أولًا |
| `GET /api/admin/plans/[id]` | **جديد**: تفاصيل + `dependencies{activeSubscriptions,totalSubscriptions,paymentReferences,deleteSafe}` |
| `POST /api/teacher/student-notes` | **جديد**: `{studentId,note}` → 201 / 400 (حدود) / 403 (دور) / 404 (خارج النطاق) / 429 (معدل) |
| `GET /api/teacher/student-notes` | **جديد**: `{groupId?,studentId?}` → `{notes[]}` |
| `POST /api/admin/plans` / `PATCH /api/admin/plans/[id]` / `DELETE` | بلا تغيير — أعيد استخدامها كما هي (حارس الـ DELETE الأصلي سديد) |
| كل مسارات الإشعارات | بلا تغيير |

---

## 8) الأمان والصلاحيات — ما يثبت بالاختبار
- **عزل المستخدمين:** B لا يرى إشعارات A ولا يقلب "مقروء" عندها (الـ route يحوي `{id, userId: user.id}` — جربناه بطلب حقيقي).
- **معرّف مزيف/مرفوض** = رد أمان (200 بدون أثر) — لا 500 ولا تسريب.
- **غير مسجّل** = 401، والشارة تعيد 0.
- **deep link لا يتجاوز التفويض:** الطالب غير المسجل في الكورس يرفضه `/api/lessons/[id]` (403/404) **حتى لو وصله إشعار برابط صحيح** — التفويض في الوجهة لا في الرابط.
- **رابط مرفوض عند الكتابة:** URLs خارجية / protocol-relative / paths / `javascript:` / تسميات قديمة → 400 `INVALID_NOTIFICATION_LINK`.
- **تعليقات المعلم:** 404 متطابق لطالب آخر معلم / طالب غير موجود؛ GET لا يقرأ خارج المجموعات؛ 403 لدور الطالب.
- **باقات:** الرفض عند الإرسال **والموافقة** (طبقتان مستقلتان).
- **الحذف الآمن:** 409 مع العدّاد الحقيقي؛ الـ orphan فقط يُحذف.

---

## 9) i18n — مفاتيح جديدة (كلها في `i18n-dict-2026.ts` — المسودة اليدوية)
| المجموعة | المفاتيح |
|----------|----------|
| `api.299–302` | رفض خارج النطاق / حدود التعليق / عنوان+نص إشعار الأب (باستيفان `{p1} {p2}`) |
| `teacher.205–214` | عنوان الـ dialog / إرشاد / placeholder / إرسال / فارغ / نجاح / فشل / عمود / إظهار السجل / تحذير "لا حذف" |
| `plan.001–033` | إنشاء/تعديل/حذف / حقول / شارات حالة / سبب الرفض / **`plan.023` "غير متاحة حاليًا"** / رسائل UI |
| `plan.034–044` | (v2) landing: قائمة فارغة / فشل تحميل / retry / شارة العرض / **7 مميزات مشتركة للبطاقات** (حصص مباشرة، تسجيلات، اختبارات، PDF، واجبات، لوحة ولي الأمر، امتحانات تجريبية) + `landing.060` (تقارير شهرية — موجود أصلًا) |
| `foot.001–003` | دعم تقني / دعم المعلمين / دعم الاشتراكات |
| `notif.*` (16) | لوحة مشتركة: رجوع/تعليم الكل/فارغ+تلميح/خطأ+retry/فتح/غير مقروء/مقروء/عدد/عنواني/الآن/قبل دقيقة/قبل ساعة/قبل يوم |

**لا تكرار رقم مفتاح** (تحقق آلي: `api.*` شاغر من 299، `teacher.*` من 205، و`plan/foot` نطاقات جديدة). لا نص عربي/إنجليزي مضمّن في الكود الجديد — كل ما يُعرض يمر عبر `t()`.

---

## 10) التجارب الجديدة — `tests/post-launch-notifications.test.js`
نصفان (بنفس نمط 26C/26D/26E):
1. **source pins** لكل إصلاح (18 مجموعة assertions) — لا يمكن لأي refactor لاحق أن يسقط أصلًا منها بصمت.
2. **`scripts/verify-post-launch-notifications.mjs`** — 106 ادعاءات ضد **قاعدة SQLite حقيقية** (base DDL + كل الهجرات الفعلية) بالهااندلرز **المُجمّعة من `src/`** بت `tsc` الخاص بالمستودع (بدون mocks للأجسام):
   - D: عقد الـ deep link النقي (8 حالات رفض + قنن).
   - E: سلسلة الأدمن→المعلم كاملة (إدراج → عداد → عرض → قراءة → تراجع العداد).
   - F: مستخدم/آباء/مجموعة/معلمين — **المجموعة لا تصل لطلاب غيرها**.
   - G: عزل كامل + 401 + معرّف مزيف.
   - H: رفض الروابط عند الكتابة + تخزين القنن.
   - I: الوجهة ترفض المحتوى حتى مع رابط صحيح.
   - J: تفضيلات (إيقاف الإشعارات + ساعات الهدوء) + breakdown في الرد.
   - K: تعليقات المعلم (19 ادعاءً: نطاق/عزل/حدود/دور/سجل/إشعار الأب).
   - L: دورة الباقات (31 ادعاءً: إنشاء/تعديل/إغلاق/ظهور/رفض شراء/إعادة تفعيل/صلاحية قائمة/409/حذف orphan/سجل سليم **+ عقد الـ landing**: السعر/الاسم الجديد يظهران في المصدر العام `L6b/L6c/L6d`، إعادة التفعيل تعيد التوفر `L12b`، الباقة المرجعية تبقى سليمة بعد رفض الحذف `L23b/L23c`، والـ public API لا يحمل حقل اعتمادات داخلي `L23d`).
   - **pins إضافية (v2):** `PricingSection` يجلب `GET /api/subscription-plans`، لا قائمة باقات ثابتة، لا اسم "Early Bird" مضمّن، CTA معطّلة للمغلقة، حالات loading/فارغ/فشل بأسماء i18n، **لا fallback سعر وهمي عند الفشل**، والـ public API بلا `subscription.count`/`payment.count`.

**نتيجة: 106/106.**

---

## 11) إعادة تشغيل السويتات الكاملة (إجماليات دقيقة — v2)
`node --test tests/*.test.js tests/*.test.mjs` → **55 من 58 ناجح**.

| السويت | النتيجة |
|--------|---------|
| `post-launch-notifications` (الجديد) | **106/106** |
| `phase26b-student-flow` (طالب/حجز/باقات) | **40/40** |
| `phase26c-admin-full-flow` (دورة حياة الأدمن) | **PASS** (كل الـ pins + الـ full-flow verifier) |
| `phase26a-public-auth` (عام/مصادقة) | **121/121** |
| `authorization-invariants` (أمان) | **93/93** |
| `auth-cross-role-phase26f` (أمان/أدوار متقاطعة) | **20/20** |
| `session-notifications-phase17` (إشعارات) | **323/323** |
| `student-locked-curriculum-phase16` | **366/366** |
| `admin-publishing-phase15` | **386/386** |

الفشل 3 **موجود من قبل** (تأكدت على HEAD النظيف بلا أي تغييراتي):
- `final-integration-phase22` / `group-track-recovery-postgres` / `pg-baseline-inspection` — كلها تتطلب **خادم PostgreSQL** غير متوفر في هذه البيئة (تعمل في CI الذي يرفع DB).
- **كل سويتات الحجز/الرفض الخادمية (`phase26b` A1 + الـ verifier) خضراء** بعد تغيير عقد قائمة الباقات.

### هجرات تجارب قديمة (تغيّر عقد متعمد من صاحب المشروع — موثق ومبرر):
| السويت | الأصل | الجديد |
|--------|-------|--------|
| phase17 §H | "المركز يدفع deep link عند النقر" مضمّن في student-dashboard | نفس العقد على **اللوحة المشتركة** + pin أن الطالب يستهلكها |
| phase16 §I1–I3 | نفس الأمر + مفتاح `student.247` | على اللوحة المشتركة + `notif.open` |
| phase15 §M3 | whitelist مضمّن في app-shell | على `view-roles.ts` + pin أن app-shell يستهلكه |
| phase26b A1 + §STUDENT-05 + §8 | "الباقات المغلقة تختفي من القائمة" | **العقد الجديد**: تظهر بوسم `isActive:false` والنشطة أولًا — **ومعها** بقى pin "المغلقة غير قابلة للشراء (400)" |

> ملاحظة صادقة: تعديل تجارب phase16/17/15/26b لم يكن "إضعاف توقعات" — كان **توثيق تغيّر عقد وافق عليه صاحب المشروع** (إخفاء الباقات المغلقة ↔ إظهارها "غير متاحة") أو **نقل التثبيت لموقعه الجديد** بعد التوحيد (نفس المقصود، أقوى). كل القيود الأمنية الأصلية بقيت مثبتة.

---

## 12) TypeScript وLint
- **`tsc --noEmit`:** فرق عن baseline = **صفر أخطاء جديدة** (الـ 64 خطأ الموجودة كلها من عميل Prisma غير المُولَّد — `binaries.prisma.sh` محجوبة في هذه البيئة؛ تُعالج في CI حيث يعمل `prisma generate`).
- **ESLint:** 42 مشكلة (41 خطأ + تحذير) **قبل وبعد** — نفس الأنماط الموجودة (`react-hooks/set-state-in-effect`)؛ **صفر جديد**.
- **المigrations:** لم تُنشئ ولم يُشغّل شيء — الميزة الجديدة تستخدم مخططًا قائمًا (`TeacherNote` موجود منذ Phase 20).

---

## 13) الأداء
- الجلب: **مرة واحدة** عند فتح اللوحة + fetch خفيف للعداد؛ لا intervals، لا refetch-on-focus، لا polling.
- لوحة الأدمن: لا طلبات إضافية (نفس الـ fetchات الموجودة + بطاقة "إشعاراتي" تشارك نفس الـ endpoint).
- Dashboard الأب: الطلب الثقيل **معطّل** أثناء عرض الإشعارات.
- لوحة الباقات للأدمن: طلب واحد إضافي (dependencies) **عند فتح dialog الحذف فقط**.

---

## 14) حالات حافة مُجرَّبة
| الحالة | السلوك |
|--------|--------|
| إشعار برابط قديم (`student-homework`) | معلومة فقط — لا زر فتح، النقر يقرأه |
| إشعار `lesson:<id>` لمعلم/أب | لا زر فتح (الرأي غير قانوني لهم) — النقر يقرأه |
| إشعار `lesson:<id>` لغير مسجل في الكورس | زر متاح لكن الوجهة ترفض (403/404) |
| أب بإشعارات موقوفة يستلم تعليق معلم | **يرى التعليق في dashboard** بلا إشعار (المصدر=الصفحة، الإشعار=ملاءمة) |
| أب بساعات هدوء | يُتخطى البث + يظهر في `skipped.quietHours` |
| باقة أُغلقت أثناء طلب شراء قائم | الرفض عند **الموافقة** (PLAN_NOT_FOUND) |
| حذف باقة لها مرجع مدفوعات | 409 + "عطّلها بدل الحذف" + السجل سليم |
| مصادقة باطلة على أي مسار جديد | 401/403 |
| 2001 حرف في تعليق معلم | 400 (api.300) — لا اقتصاص صامت |

---

## 15) خارج النطاق (مُوثق عمدًا)
> **ملاحظة v2:** البند السابق 1 (`PricingSection`) **لم يعد خارج النطاق** — نُفّذ ضمن هذا الإصدار وأصبح الـ landing يقرأ المصدر القياسي (انظر §6). باقي البنود سارية.

1. **مستقبل التفعيل** (للمعلم): تفعيل/تعطيل حسابات المعلمين موجود أصلاً في "المستخدمون"؛ ما أُنشئ هنا هو الميزة الجديدة (التعليقات) — لا حاجة لإعادة بناء.
2. **`retired` كحالة ثالثة** — لا يوجد في المخطط ولا في أي مسار؛ **لم يُخترع** (القرار: الإغلاق هو حالة الإيقاف الوحيدة، والحذف الفعلي للـ orphan فقط).
3. **Cron / منتِج إشعارات مجدول** — لا يوجد في الكود (خريطة المنتِجين موثقة في التحليل)؛ لم يُخترع.
4. **WhatsApp/Email كقناة** لإشعارات الأب — الإشعار **في التطبيق** (نمط Phase 17)؛ خارج الطلب.
5. **سمات/مميزات مختلفة لكل باقة** — المخطط لا يحمل قائمة مميزات لكل باقة؛ البطاقات تعرض قائمة مميزات **مشتركة واحدة** (كل الباقات تشمل المنصة كاملة، والاختلاف = المدة/السعر/الوصف المُدار من الأدمن). لم يُخترع حقل مميزات.

---

## 16) ما يلزم للمراجعة والإنتاج
- **الإنتاج:** لا هجرات مطلوبة — النشر = الكود فقط. (لم تلمس قاعدة الإنتاج إطلاقًا؛ كل الاختبارات على قاعدة مؤقتة.)
- **PR:** **لم يُفتح** — بانتظار مراجعة هذا التقرير (تعليمات صريحة).
- **توصية متابعة (ليست عيب):** `PricingSection` و`enroll-view` يظهران السعر بـ `pay.egp` — إن وُجدت أسعار غير EGP مستقبلًا يلزم توسعة؛ لا حاجة اليوم.
- **توصية جودة:** تثبيت `react-hooks/set-state-in-effect` في قاعدة Lint (41 حالة موجودة عبر الكود) كـ warning→error تدريجي.

### الملفات (إجمالي v2)
- **جديد (5):** `src/lib/view-roles.ts` · `src/components/shared/notifications-panel.tsx` · `src/app/api/teacher/student-notes/route.ts` · `scripts/verify-post-launch-notifications.mjs` · `tests/post-launch-notifications.test.js`
- **معدّل (17):** `store.ts` · `shell.tsx` · `app-shell.tsx` · لوحات الطلاب/المعلم/الأب/الأدمن · `enroll-view.tsx` · `sections.tsx` (الفوتر **+ PricingSection من المصدر القياسي**) · `i18n-dict-2026.ts` · `subscription-plans/route.ts` · `admin/plans/[id]/route.ts` · 4 سويتات قديمة (هجرة تثبيتات) · `verify-phase26b-student.mjs` (هجرة عقد متعمد)

**الإجمالي مقابل main:** 22 ملفًا (5 جديد + 17 معدّل) · **+3149 / −431 سطر**.
